# lean4-skills-lite

## Lean lemma/definition search

Use `lean-search` before guessing any Lean's stdlib and mathlib name — lemmas, definitions,
tactics, type classes, notation, anything from Mathlib/Init/Std/Batteries.
The skill has multiple backends (semantic, loogle, leanfinder, etc.) for
different query shapes.
